import Student from './domain/Student'
import FormLabel from '@mui/material/FormLabel'

interface Props {
  student: Student | null
}
const StudentDetails = ({ student }: Props) => null

export default StudentDetails
